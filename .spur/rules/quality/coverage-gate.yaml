# Coverage gate — per-file line coverage meets threshold from Bun's lcov output.
# Adapted from ts-libs, re-scoped to ts-base's layout:
#   - lcovPath kept at .coverage/lcov.info (ts-base's `bun run test` writes
#     coverage there via --coverage-dir=.coverage)
#   - include covers scripts/ + scaffold source
#   - threshold kept at 90 matching bunfig.toml coverageThreshold
rules:
  - id: coverage-gate
    description: "Per-file line coverage meets 90% threshold (read from lcov)"
    severity: error
    evaluator:
      type: coverage-gate
      config:
        lcovPath: .coverage/lcov.info
        threshold: 90
        include:
          - "scripts/**"
          - "src-app/src/**"
          - "src-lib/src/**"
          - "src-cli/**/src/**"
          - "src-monorepo/**/src/**"
        exclude:
          - "**/node_modules/**"
          - "**/vendors/**"
          - "**/dist/**"
          - "**/tests/**"
          - "**/stubs/**"
