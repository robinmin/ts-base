# Spur project configuration for ts-base
# Generated by `spur init`

version: "1"
name: ts-base

agent:
  default: pi

rules:
  paths:
    - .spur/rules/**/*.yaml

workflows:
  paths:
    - .spur/workflows/

redaction:
  enabled: false
